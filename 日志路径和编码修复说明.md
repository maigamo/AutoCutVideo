# 日志路径和编码修复说明

## 已修复的问题

### 问题一：日志路径修改 ✅

**原问题：**
- 日志存储在用户AppData目录：`C:\Users\{用户名}\AppData\Roaming\autocutvideo-electron\logs`
- 不符合将所有文件集中在项目目录的使用习惯

**解决方案：**
- 将日志路径改为项目根目录下的 `logs` 文件夹
- 开发环境：`{项目根目录}/logs`
- 生产环境：`{可执行文件目录}/logs`

**修改的文件：**

1. **src/main/services/ConfigService.ts**
   - 在 `logging` 配置中添加 `logDir` 字段
   - 默认值设置为 `path.join(projectRoot, 'logs')`

2. **src/main/utils/logger.ts**
   - 添加 `getProjectRoot()` 函数
   - 修改默认日志目录为项目根目录
   - 在所有 DailyRotateFile 配置中添加 UTF-8 编码选项

3. **test-logs.ps1**
   - 修改日志目录检测逻辑，指向项目根目录
   - 更新所有日志查看命令为相对路径

4. **启动项目.ps1**
   - 修改日志目录提示为 `.\logs\`
   - 更新日志查看命令示例

### 问题二：中文乱码修复 ✅

**原问题：**
```
[2025-11-13 20:25:05.812] [INFO ] [ipc] 鎵€鏈塈PC澶勭悊鍣ㄥ凡娉ㄥ唽
```

**原因：**
- 日志文件写入时使用了非UTF-8编码
- PowerShell读取日志时编码不匹配

**解决方案：**

1. **日志文件写入（logger.ts）**
   - 在所有 `DailyRotateFile` 配置中添加：
     ```typescript
     options: { flags: 'a', encoding: 'utf8' }
     ```
   - 确保日志文件以UTF-8格式写入

2. **PowerShell读取（查看日志时）**
   - 使用 `-Encoding UTF8` 参数：
     ```powershell
     Get-Content .\logs\app-*.log -Tail 20 -Wait -Encoding UTF8
     ```

## 新的日志路径结构

```
AutoCutVideo-dev/
├── logs/                          # 日志目录（新位置）
│   ├── app-2025-11-14.log        # 应用主日志
│   ├── error-2025-11-14.log      # 错误日志
│   └── download-2025-11-14.log   # 下载日志
├── videos/
│   ├── raw/                       # 下载的原始视频
│   ├── edited/                    # 剪辑后的视频
│   └── temp/                      # 临时文件
└── music/                         # 背景音乐
```

## 如何使用

### 1. 启动项目

```powershell
.\启动项目.ps1
```

项目启动后，日志会自动写入到 `.\logs\` 目录。

### 2. 查看日志（推荐方式）

#### 方式A：使用查看脚本（交互式）

```powershell
.\view-logs.ps1
```

然后选择要查看的日志类型：
1. 应用日志（所有）
2. 错误日志
3. 下载日志
4. 运行验证脚本

#### 方式B：直接查看（带UTF-8编码）

```powershell
# 查看应用日志（实时更新）
Get-Content .\logs\app-*.log -Tail 20 -Wait -Encoding UTF8

# 查看错误日志
Get-Content .\logs\error-*.log -Tail 20 -Wait -Encoding UTF8

# 查看下载日志
Get-Content .\logs\download-*.log -Tail 20 -Wait -Encoding UTF8
```

#### 方式C：使用验证脚本

```powershell
.\test-logs.ps1
```

### 3. 在VSCode中查看（推荐）

VSCode默认使用UTF-8编码，可以直接打开日志文件查看，不会有乱码问题。

```powershell
# 在VSCode中打开日志
code .\logs\app-2025-11-14.log
```

## PowerShell版本兼容性

### PowerShell 5.1（Windows默认）

```powershell
# 不支持 -Encoding UTF8 参数，需要使用替代方案
Get-Content .\logs\app-*.log -Tail 20 | Out-String
```

### PowerShell 7+（推荐）

```powershell
# 支持 -Encoding UTF8 参数
Get-Content .\logs\app-*.log -Tail 20 -Wait -Encoding UTF8
```

### 如何检查PowerShell版本

```powershell
$PSVersionTable.PSVersion
```

### 如果显示乱码

```powershell
# 方法1：设置控制台编码
chcp 65001
Get-Content .\logs\app-*.log -Tail 20

# 方法2：使用VSCode查看（推荐）
code .\logs\

# 方法3：使用验证脚本（自动处理编码）
.\test-logs.ps1
```

## 修改说明

### ConfigService.ts 修改

```typescript
// 添加了 logDir 配置项
logging: {
  level: process.env.NODE_ENV === 'development' ? 'debug' : 'info',
  maxFileSize: 20,
  maxFiles: 365,
  consoleOutput: true,
  logDir: path.join(projectRoot, 'logs') // 新增：日志目录
}
```

### logger.ts 修改

```typescript
// 1. 添加了 getProjectRoot() 函数
function getProjectRoot(): string {
  if (process.env.NODE_ENV === 'development') {
    return process.cwd()
  }
  return path.dirname(app.getPath('exe'))
}

// 2. 修改默认日志目录
const defaultConfig: Required<LoggerConfig> = {
  logDir: path.join(getProjectRoot(), 'logs') // 改为项目根目录
}

// 3. 在所有日志文件配置中添加UTF-8编码
new DailyRotateFile({
  // ... 其他配置
  options: { flags: 'a', encoding: 'utf8' } // 新增：UTF-8编码
})
```

## 验证修复

### 1. 检查日志路径

```powershell
# 运行验证脚本
.\test-logs.ps1
```

应该看到：
```
Found log directory: C:\open_workspace\AutoCutVideo-dev\logs
```

### 2. 检查中文显示

```powershell
# 使用UTF-8编码查看
Get-Content .\logs\app-*.log -Tail 5 -Encoding UTF8
```

应该看到正常的中文，例如：
```
[2025-11-14 10:30:00.123] [INFO ] [ipc] 所有IPC处理器已注册
[2025-11-14 10:30:00.124] [INFO ] [main] IPC处理器初始化成功
```

### 3. 查看文件编码

```powershell
# 使用VSCode打开
code .\logs\app-*.log

# 在VSCode右下角应该显示：UTF-8
```

## 新增工具

1. **view-logs.ps1** - 交互式日志查看工具
   - 提供菜单选择要查看的日志类型
   - 自动使用UTF-8编码
   - 支持实时更新

2. **test-logs.ps1** - 日志验证工具（已更新）
   - 检查日志目录（项目根目录）
   - 列出日志文件
   - 预览最新日志
   - 统计日志信息
   - 提供查看命令

3. **启动项目.ps1** - 启动脚本（已更新）
   - 显示正确的日志路径
   - 提供UTF-8编码的查看命令

## 常见问题

### Q1: 为什么改到项目根目录？

**A:** 
- 方便管理：所有项目文件集中在一起
- 便于备份：复制项目文件夹即可
- 符合开发习惯：日志与代码在同一位置
- 容易查找：不需要去AppData目录找

### Q2: 旧的日志文件怎么办？

**A:** 
旧的日志仍在 `C:\Users\{用户名}\AppData\Roaming\autocutvideo-electron\logs`
可以手动删除或保留作为备份。

### Q3: 中文还是乱码怎么办？

**A:** 
1. 使用VSCode打开日志文件（推荐）
2. 运行 `.\view-logs.ps1` 查看
3. 升级到PowerShell 7
4. 使用 `chcp 65001` 设置编码后再查看

### Q4: 如何恢复到旧的路径？

**A:** 
修改 `src/main/utils/logger.ts`：
```typescript
logDir: path.join(app.getPath('userData'), 'logs')
```

## 总结

✅ 日志路径已改为项目根目录 `.\logs\`
✅ 日志文件以UTF-8编码写入
✅ 提供了UTF-8编码的查看命令
✅ 更新了所有相关脚本和文档
✅ 新增了便捷的查看工具

所有修改都已完成，现在日志功能更加符合使用习惯，且中文显示正常！

