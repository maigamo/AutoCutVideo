# 开发环境说明

## 关于 window.api 未定义的问题

### 问题原因

在开发环境中，如果您直接在浏览器中访问 `http://localhost:10031`，会遇到 `window.api is undefined` 的错误。这是因为：

1. **Electron 的 contextBridge** 只在 Electron 应用中生效
2. **浏览器环境** 无法访问 Electron 的 preload 脚本
3. **开发模式** 下前端代码会先于 Electron 窗口加载

### 解决方案

项目已经添加了防御性检查：

```typescript
// 检查 API 是否可用
if (!window.api) {
  console.warn('Electron API 未就绪，使用默认配置')
  return
}
```

### 正确的测试方式

#### 方式一：使用 Electron 应用（推荐）

```powershell
# 启动完整的 Electron 应用
.\启动项目.ps1
```

这将启动完整的 Electron 应用，包括：
- 主进程（Main Process）
- 渲染进程（Renderer Process）
- Preload 脚本（暴露安全的 API）

#### 方式二：仅开发前端界面

如果您只想开发前端界面样式，可以：

1. 注释掉依赖 `window.api` 的代码
2. 使用模拟数据进行开发
3. 最后在 Electron 中测试完整功能

## 开发流程建议

### 1. 前端界面开发

```powershell
# 仅启动前端开发服务器
cd src/renderer
npm run dev
```

此时可以在浏览器中快速预览界面，但无法使用 IPC 功能。

### 2. 功能开发和测试

```powershell
# 启动完整的 Electron 应用
.\启动项目.ps1
```

此时可以测试完整功能，包括：
- IPC 通信
- 数据库操作
- 文件系统访问
- 视频下载
- 视频剪辑

### 3. 日志查看

```powershell
# 实时查看日志
Get-Content logs\app-2025-11-14.log -Wait -Tail 50
```

## 常见问题

### Q1: window.api 未定义

**A:** 确保您是在 Electron 应用中测试，而不是直接在浏览器中访问。

### Q2: 修改代码后没有生效

**A:** 
1. 主进程代码修改后需要重启整个应用
2. 渲染进程代码修改后会热重载（HMR）
3. Preload 脚本修改后需要重启应用

```powershell
# 停止所有 Electron 进程
Stop-Process -Name electron -Force

# 重新启动
.\启动项目.ps1
```

### Q3: 日志中文显示乱码

**A:** 这是 PowerShell 的编码问题，不影响功能。可以：
1. 使用 VSCode 终端查看日志文件（会正确显示中文）
2. 或者修改 PowerShell 的编码：`[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`

## 架构说明

```
┌─────────────────────────────────────────┐
│           Electron 主进程                │
│  - 数据库管理                            │
│  - IPC 处理器                            │
│  - Worker（下载、剪辑、导出）            │
│  - 日志记录                              │
└──────────────┬──────────────────────────┘
               │ IPC 通信
┌──────────────┴──────────────────────────┐
│         Preload 脚本                     │
│  - contextBridge.exposeInMainWorld()    │
│  - 暴露安全的 window.api 接口            │
└──────────────┬──────────────────────────┘
               │
┌──────────────┴──────────────────────────┐
│         渲染进程（Vue 3）                │
│  - 用户界面                              │
│  - 通过 window.api 调用主进程功能        │
└─────────────────────────────────────────┘
```

## 调试技巧

### 1. 开启开发者工具

主进程中已经配置了自动开启：

```typescript
if (process.env.NODE_ENV === 'development') {
  mainWindow.webContents.openDevTools()
}
```

### 2. 查看 window.api

在 DevTools Console 中：

```javascript
console.log(window.api)
// 应该输出: { invoke: ƒ, on: ƒ, off: ƒ, send: ƒ }
```

### 3. 测试 IPC 调用

```javascript
// 测试获取设置
window.api.invoke('settings:get').then(result => {
  console.log('设置:', result)
})

// 测试创建批次
window.api.invoke('db:batch:create', {
  name: '测试批次',
  total_count: 1,
  edit_config: {}
}).then(result => {
  console.log('批次:', result)
})
```

## 性能优化建议

### 1. 开发模式

- 使用热重载快速预览界面变化
- 主进程日志级别设置为 `debug`
- 开启 DevTools

### 2. 生产模式

- 关闭 DevTools
- 主进程日志级别设置为 `info` 或 `warn`
- 启用硬件加速

```typescript
// 配置示例
editing: {
  defaultSpeed: 1.2,
  defaultSharpen: 0.1,
  defaultCRF: 18,
  defaultPreset: 'slow',
  muteOriginalAudio: true,
  randomMusic: true
}
```

## 相关文件

- **主进程配置**: `src/main/index.ts`
- **Preload 脚本**: `src/preload/index.ts`
- **前端类型定义**: `src/renderer/src/types/window.d.ts`
- **构建配置**: `electron.vite.config.ts`

## 下一步

1. ✅ 项目已启动并正常运行
2. ✅ 在 Electron 窗口中测试功能（不是浏览器）
3. ✅ 查看日志确认 IPC 调用成功
4. ✅ 测试视频下载功能

---

**最后更新**: 2025-11-14  
**开发环境**: Windows 10, Node.js 18+, Electron 28+

