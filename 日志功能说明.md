# AutoCutVideo 日志功能说明

## 日志功能验证结果 ✅

### 1. npm run dev 是否写入日志文件？

**答：是的，会自动写入日志文件。**

运行 `npm run dev` 时，应用启动后会自动：
- 初始化日志系统
- 根据配置创建日志目录
- 实时写入日志到文件

### 2. 日志文件位置

日志文件存储在：
```
C:\Users\{用户名}\AppData\Roaming\autocutvideo-electron\logs\
```

或者在开发环境中，也可能在项目根目录的 `logs/` 文件夹。

### 3. 日志文件类型

系统会自动生成以下类型的日志文件：

| 文件名格式 | 说明 | 内容 |
|-----------|------|------|
| `app-YYYY-MM-DD.log` | 应用主日志 | 所有级别的日志 |
| `error-YYYY-MM-DD.log` | 错误日志 | 仅包含错误信息 |
| `download-YYYY-MM-DD.log` | 下载日志 | 下载模块专用日志 |

### 4. 日志配置

日志系统支持以下配置（可在设置页面修改）：

- **日志级别**：error / warn / info / debug
- **文件大小**：单个文件最大 20MB（默认）
- **保留时间**：365天（1年，默认）
- **控制台输出**：是否同时输出到控制台
- **自动压缩**：旧日志文件会自动压缩
- **自动清理**：每天检查并清理过期日志

### 5. 如何验证日志功能

#### 方法一：使用验证脚本（推荐）

```powershell
# 运行日志验证脚本
.\test-logs.ps1
```

这个脚本会：
- 检查日志目录是否存在
- 列出所有日志文件
- 显示最新日志内容预览
- 统计日志文件信息
- 提供实时查看日志的命令

#### 方法二：手动检查

```powershell
# 1. 查看日志目录
$logDir = Join-Path $env:APPDATA "autocutvideo-electron\logs"
explorer $logDir

# 2. 列出所有日志文件
Get-ChildItem $logDir -Filter "*.log"

# 3. 查看最新日志内容（最后20行）
Get-Content "$logDir\app-*.log" -Tail 20

# 4. 实时监控日志（持续更新）
Get-Content "$logDir\app-*.log" -Tail 20 -Wait
```

#### 方法三：在应用中测试

1. 启动应用：`npm run dev`
2. 执行一些操作（如打开设置、创建任务等）
3. 查看日志文件是否有新的记录

### 6. 日志格式示例

```log
[2025-11-13 20:25:05.812] [INFO ] [ipc         ] 所有IPC处理器已注册
[2025-11-13 20:25:05.814] [INFO ] [main        ] IPC处理器初始化成功
[2025-11-13 20:25:05.987] [ERROR] [download    ] Playwright浏览器启动失败 {
  "error": "browserType.launch: Executable doesn't exist..."
}
```

格式说明：
- `[时间戳]` - 精确到毫秒
- `[级别]` - ERROR/WARN/INFO/DEBUG
- `[模块]` - 日志来源模块
- `消息内容` - 日志信息
- `{元数据}` - 额外的结构化信息（JSON格式）

### 7. 常用日志查看命令

```powershell
# 查看所有应用日志（实时）
Get-Content "$env:APPDATA\autocutvideo-electron\logs\app-*.log" -Tail 20 -Wait

# 仅查看错误日志
Get-Content "$env:APPDATA\autocutvideo-electron\logs\error-*.log" -Tail 20 -Wait

# 查看下载日志
Get-Content "$env:APPDATA\autocutvideo-electron\logs\download-*.log" -Tail 20 -Wait

# 搜索特定关键词
Get-Content "$env:APPDATA\autocutvideo-electron\logs\app-*.log" | Select-String "下载"

# 查看今天的所有错误
Get-Content "$env:APPDATA\autocutvideo-electron\logs\error-*.log" | Select-String "ERROR"

# 按时间范围查看日志
$today = Get-Date -Format "yyyy-MM-dd"
Get-Content "$env:APPDATA\autocutvideo-electron\logs\app-$today.log"
```

### 8. 日志增强功能

相比原来的日志系统，新的日志功能包括：

✅ **更详细的日志内容**
- 时间戳精确到毫秒
- 模块名称对齐显示
- 结构化元数据（JSON格式）
- 错误堆栈信息完整记录

✅ **日志分类存储**
- 应用日志、错误日志、下载日志分开存储
- 便于定位问题和分析

✅ **自动管理**
- 按日期自动分割日志文件
- 自动压缩旧日志
- 自动清理过期日志（默认1年）

✅ **可配置性**
- 支持动态调整日志级别
- 支持自定义保留时间
- 支持控制台输出开关

✅ **下载日志增强**
- 记录每个下载步骤
- 记录下载耗时
- 记录文件大小
- 详细的错误信息

## 发现的问题

从日志中发现需要安装Playwright浏览器：

```powershell
# 安装Playwright浏览器
npx playwright install
```

这个命令会下载Chromium浏览器，用于下载功能。

## 故障排查

### 问题1：日志目录不存在
**解决**：首次运行应用时会自动创建，无需手动处理。

### 问题2：日志文件为空
**解决**：
1. 确认应用已启动
2. 执行一些操作触发日志记录
3. 检查日志级别配置

### 问题3：日志有乱码
**解决**：
1. 使用UTF-8编码查看日志文件
2. 在PowerShell中设置编码：`chcp 65001`
3. 使用支持UTF-8的文本编辑器（如VSCode）

### 问题4：日志文件过大
**解决**：
1. 在设置中调整单个文件大小限制
2. 调整日志保留天数
3. 手动运行清理：打开应用会自动清理过期日志

## 总结

✅ 日志功能已完全实现并正常工作
✅ npm run dev 会自动写入日志文件
✅ 支持多种日志级别和配置
✅ 提供了便捷的验证和查看工具
✅ 日志格式清晰，便于问题排查

