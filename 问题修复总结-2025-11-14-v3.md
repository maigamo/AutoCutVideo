# 问题修复总结 - 2025年11月14日 (第三轮)

## 修复概述

本次修复解决了3个新问题，主要涉及CRF默认值调整、画质增强配置和文件大小显示。

---

## 问题一：设置中剪辑预设的CRF默认值改为0（无损）

### 问题描述
需要将CRF默认值从16改为0，以获得无损质量。

### 修复方案
1. **后端配置** (`ConfigService.ts`)
   - 将 `defaultCRF` 从 16 改为 **0**
   - 更新注释说明：0为无损质量

2. **前端配置** (`Settings.vue`)
   - 同步更新前端默认值为 0
   - 保持UI提示文案准确

### 修改文件
- `src/main/services/ConfigService.ts`
- `src/renderer/src/views/Settings.vue`

### 技术说明
- **CRF = 0**: 无损质量，文件最大但画质完美
- **CRF = 16**: 超高质量（之前的默认值）
- **CRF = 18**: 视觉无损
- **CRF = 23**: 标准质量

---

## 问题二：自动剪辑时提升画质（2K分辨率、60帧率、高码率）

### 问题描述
需要在自动剪辑时支持画质增强，包括：
- 将分辨率调整为2K (2560x1440)
- 将帧率调整为60fps
- 将码率调整为较高值（20Mbps）

### 修复方案

#### 1. 扩展EditConfig接口 (`ffmpeg.util.ts`)
```typescript
export interface EditConfig {
  // ... 原有配置
  // 画质增强配置
  resolution?: string    // 分辨率，如 '2560x1440' (2K)
  fps?: number           // 帧率，如 60
  videoBitrate?: string  // 视频码率，如 '20000k' (20Mbps)
  crf?: number           // CRF质量值 0-51
  preset?: string        // 编码预设
}
```

#### 2. 修改FFmpeg处理逻辑 (`ffmpeg.util.ts`)
- 支持动态设置输出分辨率：`command.size(config.resolution)`
- 支持动态设置输出帧率：`command.fps(config.fps)`
- 支持动态设置视频码率：`command.videoBitrate(config.videoBitrate)`
- 支持从配置读取CRF和编码预设
- 添加详细的调试日志

#### 3. 扩展配置服务 (`ConfigService.ts`)
```typescript
editing: {
  // ... 原有配置
  // 画质增强配置
  defaultResolution: '2560x1440',  // 2K分辨率
  defaultFps: 60,                   // 60帧
  defaultVideoBitrate: '20000k'     // 20Mbps
}
```

#### 4. 设置页面UI (`Settings.vue`)
添加新的配置选项：
- **输出分辨率**：下拉选择（原始/1080p/2K/4K）
- **输出帧率**：数字输入（0-120fps，0表示保持原始）
- **视频码率**：文本输入（如 20000k）

#### 5. 批量输入页面 (`BatchInput.vue`)
- 从设置中自动加载画质增强配置
- 默认使用2K、60fps、20Mbps配置

### 修改文件
- `src/main/utils/ffmpeg.util.ts`
- `src/main/services/ConfigService.ts`
- `src/renderer/src/views/Settings.vue`
- `src/renderer/src/views/BatchInput.vue`

### 技术细节

#### 分辨率选项
- **原始分辨率**：不改变（空值）
- **1920x1080**: 1080p（标准高清）
- **2560x1440**: 2K（超高清，默认）
- **3840x2160**: 4K（超高清）

#### 帧率设置
- **0**: 保持原始帧率
- **30**: 标准帧率
- **60**: 高帧率（默认，流畅）
- **120**: 超高帧率

#### 码率设置
- **5000k**: 5 Mbps（标准）
- **10000k**: 10 Mbps（高质量）
- **20000k**: 20 Mbps（超高质量，默认）
- **30000k+**: 极高质量

### 配置组合说明
当前默认配置为**极致画质模式**：
- **CRF**: 0（无损）
- **分辨率**: 2560x1440 (2K)
- **帧率**: 60fps
- **码率**: 20Mbps
- **编码预设**: veryslow（最高质量）

**注意**：此配置会：
- ✅ 产生极高画质的视频
- ⚠️ 文件体积会非常大
- ⚠️ 处理速度会很慢（veryslow预设）
- ⚠️ 需要较强的硬件性能

### 灵活配置
用户可以在设置中自由调整这些参数：
- 如果原视频已经是2K或更高，可以设置为空值保持原始分辨率
- 如果不需要60fps，可以设置为0保持原始帧率
- 如果想要更快的处理速度，可以调整编码预设为medium或fast

---

## 问题三：视频管理模块原视频也要展示文件大小

### 问题描述
视频管理模块中，原视频的大小显示为"-"，需要显示实际的文件大小。

### 问题原因
在下载完成后创建视频记录时，没有获取并保存文件大小信息。

### 修复方案
在 `download.ipc.ts` 的下载完成回调中：
1. 使用 `fs.statSync()` 获取文件统计信息
2. 提取文件大小 `stats.size`
3. 将文件大小传递给 `createVideo()` 方法

```typescript
// 获取文件大小
let fileSize = 0
try {
  const stats = fs.statSync(filePath)
  fileSize = stats.size
} catch (error) {
  logger.warn('获取文件大小失败', { module: 'download', filePath, error })
}

const videoId = dbService.createVideo({
  batch_id: task.batch_id,
  filename: path.basename(filePath),
  file_path: filePath,
  file_size: fileSize,  // 添加文件大小
  is_edited: false,
  source_task_id: taskId
})
```

### 修改文件
- `src/main/ipc/download.ipc.ts`

### 容错处理
- 如果获取文件大小失败，会记录警告日志
- fileSize 默认为 0，不会导致程序崩溃
- 前端会将 0 显示为 "-"（原有逻辑）

---

## 总体改进

### 代码质量
- ✅ 所有修改通过了 TypeScript 类型检查
- ✅ 没有 linter 错误
- ✅ 代码格式规范统一
- ✅ 添加了详细的注释和日志

### 功能增强
- ✅ 支持完全自定义的画质输出
- ✅ 提供了极致画质的默认配置
- ✅ 完善了文件信息展示

### 用户体验
- ✅ 在设置页面提供了友好的UI配置
- ✅ 每个配置项都有详细的说明
- ✅ 提供了合理的默认值和推荐值

---

## 测试建议

### 问题一测试
1. 重置设置或创建新项目
2. 进入"设置 > 剪辑预设"
3. 查看"默认CRF质量"
4. 验证默认值是否为 0

### 问题二测试

#### 基础测试
1. 进入"设置 > 剪辑预设 > 画质增强设置"
2. 验证默认配置：
   - 输出分辨率：2560x1440 (2K)
   - 输出帧率：60 fps
   - 视频码率：20000k

#### 功能测试
1. 提交一个视频链接进行下载和剪辑
2. 等待剪辑完成
3. 检查输出视频的属性：
   - 分辨率是否为2560x1440
   - 帧率是否为60fps
   - 码率是否接近20Mbps
4. 对比原视频和剪辑后视频的画质

#### 性能测试
1. 观察处理时间（预期会较长）
2. 观察文件大小（预期会很大）
3. 观察系统资源占用

### 问题三测试
1. 下载一个新视频
2. 进入"视频管理"页面
3. 查看原视频的"大小"列
4. 验证是否显示实际文件大小（如 "15.23 MB"）
5. 确认不再显示为"-"

---

## 性能和存储建议

### 当前配置的影响
使用默认的极致画质配置时：

#### 文件大小估算
- **1分钟视频**：约 150-200 MB
- **5分钟视频**：约 750-1000 MB (1 GB)
- **10分钟视频**：约 1.5-2 GB

#### 处理时间估算（参考，实际取决于硬件）
- **1分钟视频**：约 5-10 分钟
- **5分钟视频**：约 25-50 分钟
- **10分钟视频**：约 50-100 分钟

### 优化建议

#### 如果需要平衡画质和文件大小
```
CRF: 18 (视觉无损)
分辨率: 1920x1080 (1080p)
帧率: 30 (标准)
码率: 10000k (10Mbps)
编码预设: slow
```

#### 如果需要快速处理
```
CRF: 23 (标准质量)
分辨率: 1920x1080 (1080p)
帧率: 30 (标准)
码率: 5000k (5Mbps)
编码预设: medium
```

#### 如果需要4K极致画质
```
CRF: 0 (无损)
分辨率: 3840x2160 (4K)
帧率: 60
码率: 30000k (30Mbps)
编码预设: veryslow
```
**警告**: 10分钟视频可能超过 3-4 GB

---

## 后续优化建议

1. **硬件加速**
   - 考虑支持NVENC（NVIDIA）、QSV（Intel）、AMF（AMD）
   - 可以大幅提升处理速度

2. **预设模板**
   - 添加"极致画质"、"平衡"、"快速"等预设模板
   - 用户可快速切换

3. **智能分辨率**
   - 检测原视频分辨率
   - 如果原视频低于目标分辨率，不进行放大（避免画质下降）

4. **进度预估**
   - 根据视频长度和配置估算处理时间
   - 提供更准确的进度提示

---

## 相关文件清单

### 后端文件
- `src/main/utils/ffmpeg.util.ts` - FFmpeg工具类（扩展接口和处理逻辑）
- `src/main/services/ConfigService.ts` - 配置服务（添加画质配置）
- `src/main/ipc/download.ipc.ts` - 下载IPC处理器（添加文件大小）

### 前端文件
- `src/renderer/src/views/Settings.vue` - 设置页面（添加画质配置UI）
- `src/renderer/src/views/BatchInput.vue` - 批量输入（加载画质配置）

---

## 修复完成时间
2025年11月14日

## 修复统计
- **修复问题数**: 3个
- **修改文件数**: 5个
- **代码行数变更**: 约 300+ 行
- **Linter错误**: 0个
- **类型检查**: ✅ 通过

---

## 开发者备注

当前配置为**无损画质 + 2K分辨率 + 60帧率 + 20Mbps码率**，适合追求极致画质的场景。

如果用户反馈处理速度太慢或文件太大，建议：
1. 将CRF调整为18（视觉无损，文件大小可减少50%）
2. 将编码预设调整为slow或medium（速度可提升2-5倍）
3. 或提供预设模板让用户快速切换

---

**所有问题已修复完成，项目可以正常使用！** 🎉

