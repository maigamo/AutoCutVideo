# 快速使用指南

## 🚀 三步开始使用

### 第一步：环境检查

```powershell
npm run check
```

确保所有项目都显示 ✅

### 第二步：准备文件

1. **添加视频文件**
   - 将待处理视频放入 `videos/input` 目录
   - 支持格式：`.mp4`, `.mov`, `.avi`, `.mkv`, `.webm`

2. **添加音乐文件**
   - 将背景音乐放入 `music` 目录
   - 支持格式：`.mp3`, `.wav`, `.aac`, `.m4a`, `.flac`

### 第三步：开始处理

```powershell
# 方法 1: 使用快速启动脚本（推荐）
.\scripts\quick-start.ps1

# 方法 2: 直接运行处理命令
node scripts/batch-video-processor.js videos/input videos/output music
```

---

## 📺 处理效果

### 原视频 → 处理后

| 特性 | 处理前 | 处理后 |
|------|--------|--------|
| **播放速度** | 正常速度 | 1.2倍速 ⚡ |
| **画面方向** | 原始方向 | 左右镜像 🔄 |
| **画面清晰度** | 原始画质 | 锐化增强 (+10%) ✨ |
| **音频** | 原始音频 | 背景音乐 🎵 |
| **文件质量** | 原质量 | 高质量 (CRF 18) 🎯 |

---

## 🎮 实用命令

### 环境检查
```powershell
# 检查 Node.js 和 FFmpeg 是否正确安装
npm run check
```

### 处理视频
```powershell
# 基础用法
node scripts/batch-video-processor.js videos/input videos/output music

# 自定义目录
node scripts/batch-video-processor.js 我的视频 处理完成 我的音乐

# 使用快速启动（带交互式确认）
.\scripts\quick-start.ps1

# 自定义目录的快速启动
.\scripts\quick-start.ps1 -InputDir "我的视频" -MusicDir "我的音乐"
```

### 查看版本信息
```powershell
# Node.js 版本
node --version

# FFmpeg 版本
ffmpeg -version

# 项目版本
npm version
```

---

## 📊 输出文件说明

### 文件命名规则

原文件名会自动添加时间戳：

```
输入: my_video.mp4
输出: my_video_2025-11-09T14-30-45.mp4
```

### 输出参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 视频编码 | H.264 (libx264) | 最佳兼容性 |
| 音频编码 | AAC | 通用音频格式 |
| 视频码率 | 8000k (8 Mbps) | 高质量 |
| 音频码率 | 192k | CD 质量 |
| CRF | 18 | 视觉无损 |
| 采样率 | 48kHz | 专业标准 |
| 像素格式 | yuv420p | 兼容所有平台 |

---

## 🔧 常见问题快速解决

### Q: 提示找不到 ffmpeg？

**解决方案**:
```powershell
# 安装 FFmpeg
choco install ffmpeg

# 验证安装
ffmpeg -version
```

### Q: 处理速度慢？

**原因**: 使用了 `slow` 预设以保证高质量

**加速方法**:
编辑 `scripts/batch-video-processor.js`，修改：
```javascript
PRESET: 'medium',  // 从 'slow' 改为 'medium'
```

### Q: 输出文件太大？

**解决方案**:
编辑 `scripts/batch-video-processor.js`，调整：
```javascript
CRF: '23',              // 从 18 改为 23
VIDEO_BITRATE: '5000k', // 从 8000k 改为 5000k
```

### Q: 想要保留原音频？

**修改方法**:
编辑 `scripts/batch-video-processor.js`，找到音频映射部分，改为：
```javascript
// 原代码（只用背景音乐）
'-map', '1:a',

// 修改为（混合音频）
'-filter_complex', '[0:a][1:a]amix=inputs=2:duration=shortest[a]',
'-map', '[a]',
```

---

## 💡 高级用法

### 批量处理不同文件夹

```powershell
# 处理第一批
node scripts/batch-video-processor.js batch1 output1 music

# 处理第二批
node scripts/batch-video-processor.js batch2 output2 music

# 处理第三批
node scripts/batch-video-processor.js batch3 output3 music
```

### 使用不同的音乐库

```powershell
# 轻音乐风格
node scripts/batch-video-processor.js videos/input videos/output music/light

# 流行音乐风格
node scripts/batch-video-processor.js videos/input videos/output music/pop

# 古典音乐风格
node scripts/batch-video-processor.js videos/input videos/output music/classical
```

### 自定义输出文件名

编辑 `scripts/batch-video-processor.js`，修改 `generateOutputFilename` 函数：

```javascript
function generateOutputFilename(inputPath) {
  const parsed = path.parse(inputPath);
  
  // 方案1: 只添加后缀
  return `${parsed.name}_processed.mp4`;
  
  // 方案2: 添加日期
  const date = new Date().toISOString().slice(0, 10); // YYYY-MM-DD
  return `${parsed.name}_${date}.mp4`;
  
  // 方案3: 添加序号
  const index = 1; // 需要维护计数器
  return `${parsed.name}_${index.toString().padStart(3, '0')}.mp4`;
}
```

---

## 📈 性能优化建议

### 1. 硬件加速

如果你的电脑支持硬件加速，可以修改视频编码器：

```javascript
// NVIDIA GPU (需要支持 NVENC)
VIDEO_CODEC: 'h264_nvenc',

// Intel GPU (需要支持 Quick Sync)
VIDEO_CODEC: 'h264_qsv',

// AMD GPU (需要支持 AMF)
VIDEO_CODEC: 'h264_amf',
```

### 2. 并行处理

默认是逐个处理视频。如果电脑性能强劲，可以同时处理多个：

编辑 `scripts/batch-video-processor.js`，修改处理循环部分：
```javascript
// 使用 Promise.all 并行处理（谨慎使用）
const maxConcurrent = 2; // 同时处理2个视频
for (let i = 0; i < videoFiles.length; i += maxConcurrent) {
  const batch = videoFiles.slice(i, i + maxConcurrent);
  await Promise.all(batch.map(processVideo));
}
```

### 3. 预设优化对比

| 预设 | 处理速度 | 质量 | 文件大小 | 推荐场景 |
|------|----------|------|----------|----------|
| ultrafast | ⚡⚡⚡⚡⚡ | ⭐ | 大 | 实时预览 |
| fast | ⚡⚡⚡⚡ | ⭐⭐⭐ | 中 | 快速测试 |
| medium | ⚡⚡⚡ | ⭐⭐⭐⭐ | 中 | **日常使用** |
| slow | ⚡⚡ | ⭐⭐⭐⭐⭐ | 小 | **高质量输出** |
| veryslow | ⚡ | ⭐⭐⭐⭐⭐ | 最小 | 最终发布 |

---

## 🎯 使用场景示例

### 场景 1: 社交媒体短视频

```javascript
// 优化配置
SPEED_RATE: 1.5,        // 更快节奏
VIDEO_BITRATE: '5000k', // 中等质量
CRF: '23',
PRESET: 'medium',       // 快速处理
```

### 场景 2: 专业视频制作

```javascript
// 高质量配置
SPEED_RATE: 1.0,        // 原速
VIDEO_BITRATE: '12000k', // 超高码率
CRF: '16',              // 接近无损
PRESET: 'veryslow',     // 最佳压缩
```

### 场景 3: 批量快速处理

```javascript
// 效率优先
SPEED_RATE: 1.2,
VIDEO_BITRATE: '3000k', // 降低码率
CRF: '28',
PRESET: 'fast',         // 快速编码
SHARPEN_AMOUNT: 0.0,    // 跳过锐化
```

---

## 📚 更多资源

- [完整文档](docs/批量视频处理指南.md)
- [安装指南](INSTALL.md)
- [更新日志](CHANGELOG.md)
- [脚本说明](scripts/README.md)

---

## 🆘 需要帮助？

1. **查看文档**: 先查阅本指南和 README.md
2. **环境检查**: 运行 `npm run check`
3. **查看错误**: 仔细阅读终端输出的错误信息
4. **提交问题**: 在 GitHub Issues 中描述问题

---

## ✨ 小技巧

### 批量重命名输出文件

```powershell
# PowerShell 批量重命名
Get-ChildItem videos/output/*.mp4 | 
  ForEach-Object { 
    Rename-Item $_ -NewName ("new_" + $_.Name) 
  }
```

### 查看视频信息

```powershell
# 查看视频详细信息
ffprobe -v quiet -print_format json -show_format -show_streams input.mp4

# 只查看时长
ffprobe -v quiet -show_entries format=duration -of csv=p=0 input.mp4
```

### 测试单个视频

```powershell
# 创建临时测试目录
mkdir test_input, test_output

# 复制一个视频进行测试
copy videos/input/test.mp4 test_input/

# 运行处理
node scripts/batch-video-processor.js test_input test_output music

# 检查结果后删除
rmdir -r test_input, test_output
```

---

🎉 **祝你使用愉快！**

如有任何问题或建议，欢迎反馈！

