# AutoCutVideo 快速测试指南

## 修复内容总结

### 1. ✅ 启动脚本修复
- 修复了PowerShell脚本解析错误
- 添加了UTF-8编码支持，避免中文乱码
- 优化了输出信息显示
- 添加了日志目录提示

### 2. ✅ 日志功能完善
- 日志系统已完全实现
- npm run dev 会自动写入日志文件
- 支持实时日志记录和查看
- 提供了日志验证工具

## 快速开始

### 步骤1：安装Playwright浏览器（首次运行必需）

```powershell
npx playwright install
```

这个命令会下载Chromium浏览器，用于视频下载功能。

### 步骤2：启动项目

#### 方法A：使用启动脚本（推荐）

```powershell
.\启动项目.ps1
```

该脚本会自动：
- 检查Node.js版本
- 检查依赖
- 编译better-sqlite3
- 显示日志目录位置
- 启动开发服务器

#### 方法B：直接运行

```powershell
npm run dev
```

### 步骤3：验证日志功能

在另一个PowerShell窗口中运行：

```powershell
# 运行日志验证脚本
.\test-logs.ps1
```

或者实时查看日志：

```powershell
# 实时查看应用日志
$logDir = Join-Path $env:APPDATA "autocutvideo-electron\logs"
Get-Content "$logDir\app-*.log" -Tail 20 -Wait
```

## 验证日志功能的方法

### 方法1：使用验证脚本（最简单）

```powershell
.\test-logs.ps1
```

脚本会显示：
- ✅ 日志目录位置
- ✅ 日志文件列表
- ✅ 最新日志内容预览
- ✅ 日志统计信息
- ✅ 实时查看命令

### 方法2：手动验证

```powershell
# 1. 查看日志目录
$logDir = Join-Path $env:APPDATA "autocutvideo-electron\logs"
echo $logDir

# 2. 打开日志目录
explorer $logDir

# 3. 查看日志文件列表
Get-ChildItem $logDir -Filter "*.log"

# 4. 查看最新日志
Get-Content "$logDir\app-*.log" -Tail 20
```

### 方法3：在应用中测试

1. 启动应用
2. 打开设置页面
3. 切换到"日志配置"标签
4. 修改日志级别或其他配置
5. 保存设置
6. 查看日志文件，应该能看到配置更新的记录

## 日志文件说明

应用运行后会在以下位置生成日志：

```
C:\Users\{用户名}\AppData\Roaming\autocutvideo-electron\logs\
├── app-2025-11-13.log      # 所有日志
├── error-2025-11-13.log    # 仅错误日志
└── download-2025-11-13.log # 下载模块日志
```

## 日志配置

可以在设置页面的"日志配置"标签中调整：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 日志级别 | info | 开发环境默认debug |
| 文件大小 | 20MB | 单个文件最大大小 |
| 保留时间 | 365天 | 自动清理1年前的日志 |
| 控制台输出 | 开启 | 同时输出到控制台 |

## 常见问题

### Q1: 启动脚本显示乱码怎么办？

**A:** 已修复！新版脚本自动设置UTF-8编码。如果还有问题，运行：

```powershell
chcp 65001
.\启动项目.ps1
```

### Q2: npm run dev 会写日志文件吗？

**A:** 是的！应用启动后会自动：
1. 创建日志目录（如果不存在）
2. 初始化日志系统
3. 实时写入日志到文件
4. 按日期和类型分类存储

### Q3: 如何确认日志功能正常工作？

**A:** 运行验证脚本：

```powershell
.\test-logs.ps1
```

如果看到类似输出，说明日志功能正常：

```
[1/4] Checking log directories...
  Found log directory: C:\Users\...\logs
  Found 3 log files

[2/4] Recent log files...
  app-2025-11-13.log
  error-2025-11-13.log
  download-2025-11-13.log
```

### Q4: 如何实时查看日志？

**A:** 使用 `-Wait` 参数：

```powershell
$logDir = Join-Path $env:APPDATA "autocutvideo-electron\logs"
Get-Content "$logDir\app-*.log" -Tail 20 -Wait
```

按 `Ctrl+C` 停止查看。

### Q5: Playwright浏览器未安装怎么办？

**A:** 运行以下命令安装：

```powershell
npx playwright install
```

这是视频下载功能必需的。

## 路径配置验证

新版路径配置支持：
- ✅ 默认路径：项目根目录下的子目录
- ✅ 自定义路径：用户可以自行选择
- ✅ 路径显示：自定义时显示完整路径，否则显示"默认"
- ✅ 恢复默认：可以一键恢复为默认路径

### 测试路径配置

1. 启动应用
2. 打开设置页面
3. 切换到"路径配置"标签
4. 应该看到4个路径配置项（下载/输出/音乐/临时）
5. 每个都显示"默认路径"或"自定义路径"
6. 点击"选择"按钮可以自定义路径
7. 点击"恢复默认"可以还原

## 测试完整流程

### 1. 环境准备
```powershell
# 安装Playwright浏览器（首次）
npx playwright install
```

### 2. 启动应用
```powershell
# 使用启动脚本
.\启动项目.ps1

# 或直接运行
npm run dev
```

### 3. 验证日志（新开窗口）
```powershell
# 运行验证脚本
.\test-logs.ps1

# 或实时查看
$logDir = Join-Path $env:APPDATA "autocutvideo-electron\logs"
Get-Content "$logDir\app-*.log" -Tail 20 -Wait
```

### 4. 测试功能
- 打开设置页面
- 修改路径配置
- 修改日志配置
- 查看日志文件是否有新记录

## 成功标志

✅ 应用能正常启动
✅ 日志文件自动创建
✅ 日志内容实时写入
✅ 设置页面显示正常
✅ 路径配置可以自定义
✅ 日志级别可以调整

## 相关文件

- `启动项目.ps1` - 项目启动脚本
- `test-logs.ps1` - 日志验证脚本（英文版）
- `日志功能说明.md` - 详细的日志功能文档
- `快速测试指南.md` - 本文档

## 技术细节

查看详细的实现细节和日志格式说明：
```powershell
Get-Content .\日志功能说明.md
```

