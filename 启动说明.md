# 🚀 AutoCutVideo 启动说明

## ⚠️ 重要：必须先为 Electron 编译 better-sqlite3

由于 better-sqlite3 是原生模块，需要针对 **Electron** 的 Node 版本编译，而不是系统的 Node.js。

---

## 🔧 解决方案

### 方式1: 直接执行命令（推荐）

```powershell
# 1. 为 Electron 重新编译 better-sqlite3
npm run rebuild

# 2. 启动开发服务器
npm run dev
```

### 方式2: 使用脚本

由于 PowerShell 执行策略限制，需要先允许脚本运行：

```powershell
# 允许当前会话运行脚本
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

# 然后执行
.\fix-and-run-simple.ps1
```

---

## 📝 详细步骤

### 第一步：为 Electron 编译 better-sqlite3

```powershell
npm run rebuild
```

这个命令会：
- 使用 `electron-rebuild` 工具
- 针对 Electron 32.2.7 (内置 Node.js 18.20.5) 编译
- 强制重新编译 better-sqlite3 模块

**预期输出**：
```
rebuilt dependencies successfully
```

### 第二步：启动开发服务器

```powershell
npm run dev
```

**预期输出**：
```
dev server running for the electron renderer process at:
  ➜  Local:   http://localhost:10031/
  ➜  Network: http://192.168.x.x:10031/

start electron app...

[INFO] [main] 数据库初始化成功
[INFO] [main] 配置加载成功
[INFO] [main] IPC处理器初始化成功
```

然后 Electron 窗口会自动打开！

---

## ❓ 为什么需要 electron-rebuild？

| 编译方式 | 针对版本 | 使用场景 |
|---------|---------|---------|
| `npm rebuild better-sqlite3` | 系统 Node.js (18.20.5) | 用于 Node.js 脚本（如 db-manager.cjs） |
| `npm run rebuild` (electron-rebuild) | Electron 的 Node (18.20.5) | 用于 Electron 应用（主进程） |

虽然版本号相同，但 **ABI 版本不同**：
- Node.js 18.20.5: NODE_MODULE_VERSION = 108
- Electron 32.2.7 内置的 Node: NODE_MODULE_VERSION = 127

所以必须使用 `electron-rebuild`！

---

## 🐛 如果还是报错

### 错误：MSBUILD : error MSB1009: 项目文件不存在

**原因**：缺少 Visual Studio Build Tools

**解决方案**：
```powershell
# 安装 windows-build-tools（需要管理员权限）
npm install --global windows-build-tools

# 或者手动安装 Visual Studio Build Tools
# 下载：https://visualstudio.microsoft.com/downloads/
# 选择 "Desktop development with C++"
```

### 错误：PowerShell 脚本被阻止

**解决方案**：
```powershell
# 临时允许（推荐）
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

# 或永久允许（需要管理员权限）
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

---

## ✅ 检查清单

在启动前确认：

- [ ] 已安装所有依赖：`npm install`
- [ ] 已为 Electron 编译 better-sqlite3：`npm run rebuild`
- [ ] 已初始化数据库：`npm run db:init`
- [ ] 端口 10031 未被占用
- [ ] Node.js 版本为 18.20.5：`node --version`

---

## 🎯 快速启动命令

复制粘贴这两行即可：

```powershell
npm run rebuild && npm run dev
```

---

**祝你使用愉快！** 🎉

